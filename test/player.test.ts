import Player from '../src/js/player';

const player = new Player();
console.log(player.foo);
console.log(player.getFoo());

describe('Name of the group', () => {
    test('should ', () => {
        expect(1).toBe(1);
    });
});
